<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="quaintcabeza.dhurihunt" >

    <application
        android:allowBackup="true"
        android:icon="@drawable/dhuri"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".ClueSeeker"
            android:label="@string/app_name"
            android:screenOrientation="portrait" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".VideoPlayer"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:label="@string/title_activity_video_player"
            android:parentActivityName=".ClueSeeker"
            android:theme="@style/FullscreenTheme" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="quaintcabeza.dhurihunt.ClueSeeker" />
        </activity>
        <activity
            android:name=".Playback"
            android:label="@string/title_activity_playback" >
        </activity>
    </application>

</manifest>
